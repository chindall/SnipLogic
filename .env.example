# Copy this to .env and fill in real values before running docker compose
# .env is gitignored — never commit it

# Strong random string for signing JWT tokens
# Generate one with: openssl rand -base64 48
JWT_SECRET=change-me-to-a-long-random-string

# PostgreSQL password (used by both the postgres container and the backend)
POSTGRES_PASSWORD=change-me-to-a-strong-password

# Frontend URL for CORS (defaults to http://localhost:5173 if not set)
# Set this to your production domain if using a reverse proxy with SSL
# FRONTEND_URL=https://sniplogic.yourdomain.com
